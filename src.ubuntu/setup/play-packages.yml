---
os_add_packages:

  # because git should be everywhere
  - git
  - cifs-utils
  - nfs-common
  - awscli

  # required for most of the dependencies
  # of the dynamic inventory scripts

os_add_pip2: []

os_add_pip3:
  - python-openstackclient

os_add_npm:
  - azure-cli
